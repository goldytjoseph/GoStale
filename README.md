# GoStale
GoStale discovers every subdomain of a target domain, probes each one for live HTTP/HTTPS content, extracts copyright year markers from the page body, and flags subdomains whose content is out of date.
